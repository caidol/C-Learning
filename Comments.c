#include <stdio.h>

int main() {

    // You use two forward slashes to run a single line comment

    /*
        You use forward slash and asterisk
        and an asterisk and forward slash
        in order
        to 
        run
        multiline
        comments
    */

   printf("I Like Pizza!\n");
   return 0;
}